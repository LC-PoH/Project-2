{
  "name": "avm-hostel-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Mobile wrapper for AVM Hostel Management System",
  "scripts": {
    "cap:init": "npx cap init \"AVM Hostel\" com.avm.hostel --web-dir=dist",
    "cap:add:android": "npx cap add android",
    "cap:copy": "npx cap copy",
    "cap:sync": "npx cap sync",
    "cap:open:android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/cli": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  }
}
